---
description: Review a pull request (or the working diff) against this package's conventions, reporting only findings worth acting on.
argument-hint: "[PR number] [--post] [--since <sha>]"
---

Follow the instructions in `.github/REVIEW.md`, treating `$ARGUMENTS` as the
arguments it describes.

That file is the canonical review specification and is deliberately
tool-agnostic, so contributors using Codex, Cursor, or anything else follow the
same one. Keep the criteria there, not here.
