% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contact-matrix-result.R
\name{flatten}
\alias{flatten}
\title{Flatten a multi-grouping contact matrix to its \verb{T x T} form}
\usage{
flatten(x)
}
\arguments{
\item{x}{a \code{contact_matrix} object as returned by \code{\link[=compute_matrix]{compute_matrix()}}}
}
\value{
a numeric \verb{T x T} matrix
}
\description{
Returns the contact matrix in the flattened representation of
Manna et al. — a \verb{T x T} matrix where each axis enumerates the
Cartesian product of grouping levels. For a single-grouping matrix
this is the matrix itself.

Row/column names join the grouping levels with a colon, e.g.
\code{"[0,5):F"}. The participant axes vary fastest in the first grouping
(column-major reshape).
}
\examples{
data(polymod)
polymod |>
  (\(s) s[country == "United Kingdom"])() |>
  assign_age_groups(age_limits = c(0, 5, 15)) |>
  compute_matrix(by = c("age", "gender")) |>
  flatten()

}
