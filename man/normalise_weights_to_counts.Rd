% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contact-matrix-utils.R
\name{normalise_weights_to_counts}
\alias{normalise_weights_to_counts}
\title{Normalise a weighted contact array to mean contacts per participant}
\usage{
normalise_weights_to_counts(
  sampled_participants,
  weighted_matrix,
  groupings = default_age_groupings()
)
}
\arguments{
\item{sampled_participants}{the sampled participants data.table
(must have \code{sampled.weight} plus the participant columns referenced
by \code{groupings})}

\item{weighted_matrix}{a rank-\verb{2K} array of weighted contact counts}

\item{groupings}{a list of grouping triples (see \code{\link[=resolve_groupings]{resolve_groupings()}});
defaults to single-age, matching pre-existing single-grouping output}
}
\value{
the array with the same \code{dim} and \code{dimnames} as
\code{weighted_matrix}
}
\description{
Divides the rank-\verb{2K} array of weighted contact counts produced by
\code{\link[=weighted_matrix_array]{weighted_matrix_array()}} by the participant-side weight totals
(cross-tabulated over the same grouping columns), giving the mean
number of contacts per participant. Cells with no participants
become \code{NA}.
}
\keyword{internal}
