% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contact-matrix-utils.R
\name{weighted_matrix_array}
\alias{weighted_matrix_array}
\title{Cross-tab contact weights over grouping columns}
\usage{
weighted_matrix_array(contacts, groupings = default_age_groupings())
}
\arguments{
\item{contacts}{the merged contacts data.table (must have
\code{sampled.weight} plus the participant/contact columns referenced by
\code{groupings})}

\item{groupings}{a list of grouping triples (see \code{\link[=resolve_groupings]{resolve_groupings()}});
defaults to single-age, matching pre-existing single-grouping output}
}
\value{
a rank-\verb{2K} array with \code{K = length(groupings)}
}
\description{
Internal helper used by \code{\link[=compute_matrix]{compute_matrix()}} and the legacy
\code{\link[=contact_matrix]{contact_matrix()}} to turn a merged contacts table into the rank-\verb{2K}
array of weighted contact counts. Each grouping contributes a
participant-side and a contact-side axis, in that order across the two
halves of the array.
}
\keyword{internal}
