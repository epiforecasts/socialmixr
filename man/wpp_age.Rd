% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wpp_age.R
\name{wpp_age}
\alias{wpp_age}
\title{Get age-specific population data according to the World
Population Prospects 2017 edition}
\usage{
wpp_age(countries, years)
}
\arguments{
\item{countries}{countries, will return all if not given}

\item{years}{years, will return all if not given}
}
\value{
Always errors.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#defunct}{\figure{lifecycle-defunct.svg}{options: alt='[Defunct]'}}}{\strong{[Defunct]}}

\code{wpp_age()} is defunct. Pass population data directly to \code{\link[=contact_matrix]{contact_matrix()}}
via the \code{survey_pop} argument instead, as a data frame with columns
\code{lower.age.limit} and \code{population}.
}
\examples{
\dontrun{
# population data now comes from a source of your choosing, for example
# the wpp2024 package (GitHub only):
# remotes::install_github("PPgp/wpp2024")
library(wpp2024)
data(popAge1dt)
uk_pop <- popAge1dt[
  name == "United Kingdom" & year == 2020,
  .(lower.age.limit = age, population = pop * 1000)
]
contact_matrix(
  polymod,
  countries = "United Kingdom",
  survey_pop = uk_pop,
  symmetric = TRUE
)
}
}
