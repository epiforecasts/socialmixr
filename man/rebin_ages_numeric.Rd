% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rebin_ages.R
\name{rebin_ages_numeric}
\alias{rebin_ages_numeric}
\title{Rebin population data to a set of age limits (numeric)}
\usage{
rebin_ages_numeric(
  pop,
  age_limits = NULL,
  pop_age_column = "lower.age.limit",
  pop_column = "population",
  ...
)
}
\arguments{
\item{pop}{a data frame with lower-age-limit and population columns (see
\code{pop_age_column} and \code{pop_column})}

\item{age_limits}{lower age limits of age groups to extract; if \code{NULL}
(default), the population data is returned unchanged}

\item{pop_age_column}{column in \code{pop} indicating the lower age group limit}

\item{pop_column}{column in \code{pop} indicating the population size}

\item{...}{ignored}
}
\value{
data frame of age-specific population data
}
\description{
Internal numeric coarsener operating on \code{lower.age.limit} columns: rebins a
population table to the age groups defined by \code{age_limits}, summing
populations when coarser groups are requested and linearly interpolating
between groups when finer ones are requested than are available. Wrapped by
\code{\link[=rebin_ages]{rebin_ages()}} and used by \code{\link[=pop_age]{pop_age()}} and the internal weighting helpers.
}
\keyword{internal}
