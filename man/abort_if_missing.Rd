% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groupings.R
\name{abort_if_missing}
\alias{abort_if_missing}
\title{Abort if grouping columns are absent from a list of available columns}
\usage{
abort_if_missing(groupings, available_cols, key, side)
}
\arguments{
\item{groupings}{a list of grouping triples as returned by
\code{\link[=resolve_groupings]{resolve_groupings()}}}

\item{available_cols}{character vector of column names that are present}

\item{key}{either \code{"part"} or \code{"cnt"}, the slot of each grouping to
check against \code{available_cols}}

\item{side}{label inserted into the error message describing which
side of the survey was checked (e.g. \code{"participant data"})}
}
\value{
invisibly \code{NULL} on success; otherwise raises a \code{cli} error
}
\description{
Internal helper used by \code{\link[=check_grouping_columns]{check_grouping_columns()}} to validate one side
(participant or contact) of a survey against a list of groupings.
Builds a single \code{cli::cli_abort()} listing every column that is missing
and, if the missing set includes the \code{"age"} grouping, hints at calling
\code{\link[=assign_age_groups]{assign_age_groups()}}.
}
\keyword{internal}
