% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rebin_ages.R
\name{align_ages}
\alias{align_ages}
\title{Align a population table to a contact matrix's grouping levels}
\usage{
align_ages(pop, x)
}
\arguments{
\item{pop}{a data frame with a \code{population} column and one column per
grouping: an \code{age} column of age-group labels for the age grouping, and a
column named after each categorical grouping holding its levels.}

\item{x}{a \code{contact_matrix} object as returned by \code{\link[=compute_matrix]{compute_matrix()}}}
}
\value{
a data frame with one column per grouping (named after the
grouping, holding the matrix's levels) plus a \code{population} column, ready
to pass as \code{survey_pop}
}
\description{
Aligns a population table to the groupings of a contact matrix produced by
\code{\link[=compute_matrix]{compute_matrix()}}, returning the \code{survey_pop} data frame that
\code{\link[=symmetrise]{symmetrise()}}, \code{\link[=split_matrix]{split_matrix()}} and \code{\link[=per_capita]{per_capita()}} expect.

The age grouping is rebinned to the matrix's age groups (via \code{\link[=rebin_ages]{rebin_ages()}},
summing) within each combination of the other groupings; the population must
be at least as fine as the matrix's age groups, otherwise \code{\link[=rebin_ages]{rebin_ages()}}
errors. Categorical groupings are aggregated to the matrix's levels by exact
name; a level not present in the matrix is an error.
}
\examples{
data(polymod)
result <- polymod |>
  (\(s) s[country == "United Kingdom"])() |>
  assign_age_groups(age_limits = c(0, 5, 15)) |>
  compute_matrix()
uk_pop <- data.frame(
  age = limits_to_agegroups(0:80, notation = "brackets"),
  population = rep(1e5, 81)
)
result |> symmetrise(survey_pop = align_ages(uk_pop, result))

}
