% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groupings.R
\name{resolve_groupings}
\alias{resolve_groupings}
\title{Resolve grouping specifications to participant/contact column pairs}
\usage{
resolve_groupings(by)
}
\arguments{
\item{by}{character vector or list of entries as described above}
}
\value{
a list of \code{list(name, part, cnt)} triples
}
\description{
Internal helper used by \code{\link[=compute_matrix]{compute_matrix()}} to translate user-facing
grouping specs into the participant- and contact-side column names that
the matrix machinery joins on.

Each entry of \code{by} may be:
\itemize{
\item the string \code{"age"} — special-cased to the package's existing
convention (\code{age.group} on participants, \code{contact.age.group} on
contacts), as produced by \code{\link[=assign_age_groups]{assign_age_groups()}}.
\item any other single string \code{"<stem>"} — resolves to \verb{part_<stem>} on
participants and \verb{cnt_<stem>} on contacts (the package's existing
convention for raw survey columns, e.g. \code{part_gender} / \code{cnt_gender}).
\item a named two-element character vector \code{c(part = "X", cnt = "Y")} — an
explicit override.
}
}
\keyword{internal}
